run = "bash build.sh && gunicorn app:app --bind 0.0.0.0:8080 --timeout 300 --workers 1"

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "bash build.sh && gunicorn app:app --bind 0.0.0.0:8080 --timeout 300 --workers 1"]
deploymentTarget = "cloudrun"
